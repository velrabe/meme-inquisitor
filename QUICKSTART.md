# 🚀 Быстрый старт

## Запуск игры

### Самый быстрый способ:
```bash
./start.sh
```

Или:
```bash
cd public
python3 -m http.server 8000
```

Затем откройте: `http://localhost:8000`

## 🎮 Управление

### Клавиатура:
- `←` `→` или `A` `D` (работает на любой раскладке) - движение

### Мобильные/Тач:
- Полупрозрачные стрелки по краям экрана
- Или зажать и перетаскивать игрока

## ⚙️ Настройка параметров

Откройте `/src/config.js` и измените:

```javascript
PLAYER_SPEED: 400,        // Скорость игрока
PLAYER_RADIUS: 50,        // Размер игрока (увеличен в 2 раза)
ENEMY_SPEED: 80,          // Скорость врагов (замедлена)
ENEMY_RADIUS: 50,         // Размер врагов (увеличен в 2 раза)
PLAYER_FIRE_DELAY: 150,   // Задержка между выстрелами (мс)
SPAWN_DELAY: 200,         // Интервал спавна врагов (мс)
SPAWN_COLUMNS: 8,         // Количество колонок для спавна (уменьшено)
ENEMY_POOL_SIZE: 150,     // Размер пула врагов (уменьшен)
```

## 🎯 Геймплей

1. Игра начинается сразу
2. Враги идут сверху вниз волнами
3. Игрок автоматически стреляет
4. Не дайте врагам дойти до красной зоны внизу
5. За каждого убитого врага +1 очко
6. Игра постепенно ускоряется

## 🏆 Счет

- **Score** - текущий счет
- **Best** - лучший результат (сохраняется в GamePush)
- При проигрыше показывается экран с кнопкой Restart

## 🔧 Решение проблем

### Изображения не загружаются?
Проверьте, что файлы `pope.png` и `sahur.png` находятся в `/public/img/`

### Игра не запускается?
Убедитесь, что:
1. Используете HTTP сервер (не открываете файл напрямую)
2. Браузер поддерживает ES6 модули
3. Нет блокировки CORS

### GamePush не работает?
Это нормально для локальной разработки. Счет сохраняется в localStorage.

---

## 📝 Полезные файлы

- `src/config.js` - все настройки игры
- `src/main.js` - главный игровой цикл
- `public/style.css` - стили и цвета

